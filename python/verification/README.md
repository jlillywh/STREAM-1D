# Moved

Verification fixtures and documentation now live at the repository root:

**[`../../verification/`](../../verification/)** — README, manifest, and [`fixtures/`](../../verification/fixtures/).

Use `verification/fixtures/` for all golden JSON/CSV benchmarks.

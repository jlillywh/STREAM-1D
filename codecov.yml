coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto
  ignore:
    - "src/wasm_bindings.rs"
    - "src/python_bindings.rs"

comment:
  layout: "reach,diff,flags,tree"
  behavior: default

# Bridge mild — synthetic Chunk 6 fixture reach

No bundled HEC-RAS geometry. Inputs are built by `bridge_mild_mapper.py` from
`verification/fixtures/bridge_bu_bd_hecras.json` (Yarnell + WSPRO BU/BD cases).

Reference terminal WSEL for oracle compare is captured from mode `0` explicit post-step
runs (`reference_wsel_bridge_mild_unsteady.json`).
